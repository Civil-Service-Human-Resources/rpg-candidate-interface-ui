
<section class="job-details">

    <header class="job-details__top">
        <div class="job-details__summary">
            <p class="back-link"><i class="icon ion-arrow-left-b" aria-hidden="true"></i><a href="{{returnUrl}}">{{__ 'vacancy.page.backToResults' }}</a></p>
            <h1 class="job-details__title">{{ vacancy.title }} <small>{{ vacancy.department.name }}</small></h1>
        </div>
        <div class="job-details__logo">
            {{#if vacancy.department.hasLogo }}
                <img class="job-details__image" src="/images/logos/department/{{ vacancy.department.id }}.gif" alt="{{ vacancy.department.name }}" />
            {{/if}}
        </div>
        <div class="job-details__apply">
            <p class="closing-date"><i class="icon ion-alert" aria-hidden="true"></i>{{__ 'global.labels.closingDate' }}: {{ vacancy.closingDate }}</p>
            <a class="button button--block" href="/apply/{{vacancy.id}}" role="button">{{__ 'global.buttons.apply' }}</a>
        </div>
    </header>

    <section class="job-details__bottom">

        <section class="job-details__meta-box job-details__meta-box--key-facts">
            <h2 class="job-details__meta-box-title text-addColon">{{__ 'vacancy.page.keyFacts' }}</h2>

            <ul class="job-details__key-facts-list">
                {{#if vacancy.id}}<li><strong>{{__ 'global.labels.jobReference' }}</strong> {{ vacancy.id }}</li>{{/if}}
                {{#if vacancy.salary}}<li><strong>{{__ 'global.labels.salaryBand' }}</strong> {{ vacancy.salary }}</li>{{/if}}
                {{#if vacancy.workingHours}}<li><strong>{{__ 'global.labels.hours' }}</strong> {{ vacancy.workingHours }}</li>{{/if}}
                {{#if vacancy.grade}}<li><strong>{{__ 'global.labels.jobGrade' }}</strong> {{ vacancy.grade }}</li>{{/if}}
                {{#if vacancy.numberVacancies}}<li><strong>{{__ 'global.labels.numVacancies' }}</strong> {{ vacancy.numberVacancies }}</li>{{/if}}
            </ul>
        </section>

        <section class="job-details__content">
            <h2>{{__ 'global.labels.jobDesc' }}</h2>

            <div>
                <h3 class="text-addColon">{{__ 'global.labels.description' }}</h3>
                <p>{{ vacancy.description }}</p>
            </div>

            {{#if vacancy.responsibilities}}
            <div>
                <h3 class="text-addColon">{{__ 'global.labels.responsibilities' }}</h3>
                <p>{{ vacancy.responsibilities }}</p>
            </div>
            {{/if}}

            {{#if vacancy.eligibility}}
                <div>
                    <h3 class="text-addColon">{{__ 'global.labels.eligibility' }}</h3>
                    <p>{{ vacancy.eligibility }}</p>
                </div>
            {{/if}}

        </section>

        <section class="job-details__meta-box job-details__meta-box--contact">
            <div id="job-details-contact">
                <h2 class="job-details__meta-box-title">{{__ 'global.labels.contactPoint' }}</h2>
                <p>
                    <span id="job-details-contactname" aria-label="{{__ 'global.labels.name' }}">{{ vacancy.contactName }}</span><br />
                    <span id="job-details-contactdept" aria-label="{{__ 'global.labels.department' }}">{{ vacancy.contactDepartment }}</span><br />
                    <span id="job-details-contactemail" class="text-wrap" aria-label="{{__ 'global.labels.email' }}"><a href="mailto:{{ vacancy.contactEmail }}">{{ vacancy.contactEmail }}</a></span><br />
                    <span id="job-details-contacttel" aria-label="{{__ 'global.labels.telephone' }}">{{ vacancy.contactTelephone }}</span>
                </p>
            </div>
        </section>

        <section class="job-details__closure">
            <div class="job-details__apply-inner">
                <p class="closing-date"><i class="icon ion-alert" aria-hidden="true"></i>{{__ 'global.labels.closingDate' }}: {{ vacancy.closingDate }}</p>
                <p><a class="button button--block" href="/apply/{{vacancy.id}}" role="button">{{__ 'global.buttons.apply' }}</a></p>
            </div>
        </section>

    </section>

</section>